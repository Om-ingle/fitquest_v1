# FitQuest Agent Context

## Codebase Metadata
- **Project Name:** FitQuest
- **Domain:** Gamified Fitness & Geospatial Territory Conquest ("King of the Hill")
- **Mobile Stack:** Kotlin, Jetpack Compose, Material 3, Voyager Navigation & ScreenModels, Orbit MVI, Room Database, Koin DI, Retrofit, MapLibre Native Android, Uber H3 Core
- **Backend Stack:** Python 3.12, FastAPI, SQLModel (SQLAlchemy + Pydantic, sync engine), psycopg2 driver, Supabase PostgreSQL, Alembic migrations, python-jose JWT (auth deferred)
- **Spatial Grid:** Uber H3 (Res 10 for dev/fast testing; Res 9 for production). Visible neighborhood: $k=2$ rings (19 hexes).

## Immutable Schemas & Contracts

### Backend SQLModel Entities
- **User:** `id` (UUID, Supabase Auth PK), `username` (str, unique), `total_lifetime_steps` (int), `total_hexes_captured` (int), `current_streak` (int), `longest_streak` (int), `last_activity_date` (date).
- **HexOwnership:** `hex_id` (str, H3 index PK), `king_id` (UUID, FK -> User.id), `defense_score_steps` (int), `captured_at` (datetime), `times_stolen` (int).
- **RunSession:** `id` (str PK), `user_id` (str, indexed), `started_at` (datetime).
- **CapturedHex:** `id` (int autoincrement PK), `run_id` (str, indexed), `hex_id` (str, indexed).
- **Quest:** `id` (UUID PK), `title` (str), `description` (str), `target_metric` (str), `target_value` (int), `reward_xp` (int).
- **UserQuest:** `user_id` (UUID, FK), `quest_id` (UUID, FK), `current_progress` (int), `is_completed` (bool), `completed_at` (datetime).
- **Friendship:** `requester_id` (UUID, FK), `addressee_id` (UUID, FK), `status` (str: pending/accepted/blocked), `created_at` (datetime).

### Mobile Persistence Schema (Room)
- **CapturedHexEntity:** `hexId` (String, PK), `totalSteps` (Int), `lastUpdated` (Long).
- **UserProfileEntity:** `id` (String, PK: "local_user"), `username` (String), `avatarName` (String), `dailyStepGoal` (Int), `level` (Int), `xp` (Int), `currentStreak` (Int), `longestStreak` (Int), `lastActiveDate` (String: YYYY-MM-DD), `totalLifetimeSteps` (Int), `totalDistanceMeters` (Double), `totalCalories` (Int), `isOnboardingCompleted` (Boolean).
- **RunSessionEntity:** `id` (String, UUID PK), `startedAt` (Long), `endedAt` (Long), `durationSeconds` (Long), `totalSteps` (Int), `distanceMeters` (Double), `caloriesBurned` (Int), `capturedHexCount` (Int), `capturedHexIdsJson` (String), `xpEarned` (Int).
- **DailyQuestEntity:** `id` (String, PK), `title` (String), `description` (String), `targetMetric` (String), `targetValue` (Int), `currentProgress` (Int), `isCompleted` (Boolean), `rewardXp` (Int), `dateString` (String).
- **AchievementEntity:** `id` (String, PK), `title` (String), `description` (String), `category` (String), `iconName` (String), `currentProgress` (Int), `maxProgress` (Int), `isUnlocked` (Boolean), `unlockedAt` (Long?).

### Network DTOs
- `POST /api/v1/runs/sync`: Request payload `RunSyncPayload(total_session_steps: Int, hexes_to_steps: Map<String, Int>)` -> Response `RunSyncSummary(hexes_defended: Int, hexes_stolen: Int, hexes_newly_captured: Int, xp_earned: Int, new_total_lifetime_steps: Int)`.
- `GET /api/v1/map/viewport`: Query params `(min_lat, min_lng, max_lat, max_lng, zoom_level)` -> Response `MapViewportResponse(is_aggregated: Boolean, hexes: List<HexDetailResponse>, heatmaps: List<HeatmapResponse>)`.

## Directory Topology
```text
fitquest/
├── apps/
│   ├── api/                     # FastAPI + SQLModel backend
│   │   ├── app/
│   │   │   ├── api/             # Global API routing & auth dependencies
│   │   │   ├── core/            # App settings, async engine, security
│   │   │   ├── main.py          # FastAPI application entry point
│   │   │   └── modules/         # Domain-driven feature modules
│   │   │       ├── map/         # Hex viewport queries and ownership service
│   │   │       ├── quests/      # Challenge definitions and progress
│   │   │       ├── runs/        # Run sync engine & turf conquest logic
│   │   │       └── users/       # User profiles and friendships
│   │   ├── alembic.ini          # DB migrations configuration
│   │   ├── requirements.txt     # Python dependencies
│   │   └── seed.py              # Test user & JWT seeder script
│   └── app/                     # Android Jetpack Compose client
│       ├── fitquest/
│       │   ├── src/main/
│       │   │   ├── java/com/example/mobileapp/
│       │   │   │   ├── FitQuestApp.kt         # Application class
│       │   │   │   ├── MainActivity.kt        # Edge-to-edge Compose host with dynamic onboarding router
│       │   │   │   ├── core/
│       │   │   │   │   ├── capture/           # HexCaptureEngine state coordinator
│       │   │   │   │   ├── data/local/        # Room DB, entities, DAOs, and local repositories
│       │   │   │   │   ├── dev/               # DevLocationSimulator, DevStepSimulator
│       │   │   │   │   ├── geo/               # HexGeoJsonMapper, UberH3HexIndexer
│       │   │   │   │   ├── network/           # FitQuestApi (Retrofit), DTO models
│       │   │   │   │   ├── permissions/       # Runtime permission helper
│       │   │   │   │   └── sensors/           # LocationTrackingManager, StepSensorManager
│       │   │   │   ├── di/AppModule.kt        # Koin DI module & dev simulation toggles
│       │   │   │   ├── features/capture/      # CaptureScreenModel (Orbit MVI), CaptureState
│       │   │   │   └── ui/                    # Onboarding, Run HUD, Home, Rank, Trophies, Profile
│       │   │   ├── jniLibs/                   # H3 native .so libraries
│       │   │   └── res/                       # Drawables, mipmaps, strings, themes
│       │   └── src/test/                      # Unit tests (HexGeoJsonMapperTest, ProgressionLogicTest)
│       └── settings.gradle.kts                # Project name: :app (mapped to dir 'fitquest')
└── docs/                                      # Technical specs, ADRs, and agent ledger
```

## Stable Architecture Requirements Log
1. **Mobile Testing & Validation:** The mobile test suite is executed from `apps/app` using `ANDROID_HOME=/home/dsk/Android/Sdk ./gradlew :app:test`. Note: Gradle root project includes `:app` (pointing to `fitquest/`), so tasks are `:app:<task>`, not `:fitquest:<task>`.
2. **Backend Execution:** The FastAPI server is executed from `apps/api` using `uvicorn app.main:app --reload --host 0.0.0.0 --port 8000`.
3. **Database Environment:** Supabase PostgreSQL via `DATABASE_URL` (env var or `.env`, never committed). Schema is managed by Alembic (`alembic upgrade head` from `apps/api`). Authentication is deferred — `get_current_user` returns a fixed dev user.
4. **Dev Simulators:** `DevLocationSimulator`/`DevStepSimulator` are dead code (not referenced anywhere) but still compiled into the release APK from `src/main`. Emulator testing uses ADB location injection; emulators lack `TYPE_STEP_COUNTER`, so step capture requires a real device.
5. **GeoJSON Offloading:** H3 coordinate-to-boundary calculations and GeoJSON string generation must always be executed on background coroutine dispatchers (e.g. `Dispatchers.Default` or `Dispatchers.IO`) to avoid frame drops on the UI thread.
6. **Git Branching Policy:** Commits must never be made directly on `main`. Feature branches must be created with explicit permission.

# ACTIVE WORKING MEMORY BLOCK (DYNAMIC SUFFIX)

## Active Working Objective
- Phase 1 (2026-09-05): Supabase PostgreSQL foundation (map provider decision: **MapLibre + MapTiler stays** — a Stadia migration was briefly applied and fully reverted):
  - Backend switched from SQLite default to env-based `DATABASE_URL` (Supabase PostgreSQL, psycopg2); fixed the broken `SQLModelasyncpg` requirement; removed the unused `supabase` package.
  - Alembic migration foundation created (`alembic/versions/0001_initial_schema.py`); no credentials in `alembic.ini`.
  - Android map implementation unchanged: MapLibre + MapTiler via `MAPTILER_API_KEY` / `MAPTILER_STYLE_URL` BuildConfig fields, OpenFreeMap Liberty keyless fallback. The Gradle `.env` candidate list does NOT include the repository root `.env` — place `MAPTILER_API_KEY` in `apps/app/.env`.
  - Known deferred issues (Phase 2+): map viewport ignores bbox; client/server XP rules differ; leaderboard is local/fake; Room uses destructive migration; GPS monitoring never stops after run ends; Android has no wired Retrofit client yet.
  - **Network constraint (2026-09-05):** the direct Supabase host `db.<ref>.supabase.co` is IPv6-only and this dev machine has no IPv6 route — use the Supavisor pooler `aws-0-ap-south-1.pooler.supabase.com:5432` (session mode, user `postgres.<ref>`) in `DATABASE_URL` for any real-DB work. (Root `.env` now uses the pooler; verified connecting.)
  - Backend `Settings` uses `extra="ignore"` so the shared root `.env` (which also holds Android-side `MAPTILER_*` vars) doesn't crash pydantic validation.
  - Phase 2 (2026-09-05): Android ↔ FastAPI run-sync wired. Retrofit/OkHttp client (`core/network/FitQuestApiClient.kt`) + error-mapping `RunSyncer` (sealed `SyncOutcome`); base URL via `BuildConfig.BACKEND_BASE_URL` (`BACKEND_BASE_URL` in `apps/app/.env`, default `http://10.0.2.2:8000/` for emulators). Run completion in `CaptureScreenModel` saves to Room first, then syncs; backend XP is authoritative and reconciles the local provisional estimate (`RunSessionEntity.isSynced`, Room migration 2→3 preserves local data). **XP mismatch fixed** — old client formula `(hexes×50)+(steps/100×10)+20` is now provisional-only. No auth headers needed (dev-user backend). No backend secrets in Android. Offline runs stay in Room, unsynced; no auto-retry.
  - Phase 4C (2026-09-05, backend only): RAG + grounded LLM coach. `app/modules/rag/` (migration 0003: ragdocument+ragchunk with pgvector `vector(1536)` + HNSW cosine index, dialect-guarded so SQLite tests skip the extension; deterministic chunker; Gemini embedding provider via REST — `gemini-embedding-001` with `outputDimensionality=1536` matching the frozen schema, NO migration churn; curated WHO/CDC corpus + `python -m app.modules.rag.ingestion` CLI) and `app/modules/coach/` (`GeminiLLMProvider` `gemini-2.5-flash`; grounded prompt = real 4A FitnessContext / retrieved knowledge / instructions; `GET /api/v1/coach` with 503/504/502 error mapping; empty retrieval → flagged `grounded:false` fallback, never fake RAG). **ONE env key: `GEMINI_API_KEY` (backend-only, owner-set 2026-09-05). LIVE E2E VERIFIED: embedding round-trip = exactly 1536 dims; migration 0003 applied to Supabase (pgvector 0.8.2 + HNSW index live); 6-doc corpus ingested with real embeddings; `GET /api/v1/coach` returns a grounded coaching message (retrieved cosine 0.60–0.65). gemini-2.5-flash thinkingBudget=0 required (default thinking truncated messages under the output cap).** httpx added to requirements.txt. All provider boundaries mocked in tests (zero API credits).
  - Phase 4C.3A (2026-09-06, backend only): retrieval-quality evaluation + threshold recalibration + coach hardening. `app/modules/rag/evaluation.py` (10 labeled coaching queries, Recall@k/hit-rate) + manual tools `tools/evaluate_retrieval.py` and `tools/evaluate_live_coach.py` (live, read-only, never in pytest). **Threshold recalibrated 0.30 → 0.50 from live evidence**: on-topic chunks 0.60–0.77 vs unrelated queries 0.43–0.46 (empty band; 0.30 admitted unrelated text). Live results: mean Recall@4 = 1.00, hit rate 1.00 (10/10); coach smoke 5/5 synthetic scenarios + real dev user all grounded/coherent/non-copying. Known quirks documented: R3 TERRITORY_AT_RISK unreachable via build_fitness_context (tested via recommend() with constructed context); LLM sometimes repeats "rules engine" phrasing; free-tier 429 on burst (→502). Backend 187 tests, tools 129.


  - Phase 4C.3B (2026-09-06, Android + one backend addition): Android AI coach integration — a real `GET /api/v1/coach` response (grounded LLM + pgvector RAG) now renders in the Home-tab Coach card, verified on a real Samsung device (RZ8R90661CF). New: `core/network/CoachFetcher.kt` (sealed Outcome, mirrors RecommendationFetcher), `CoachResponse`/`CoachRetrievalInfo` DTOs (nullable-by-design — Gson bypasses Kotlin null-safety), `FitQuestApi` GET coach, Koin single, HomeTab Coach card = TWO independent sections (unchanged Phase 4A recommendation + AI advice with own retry key/produceState — a slow/failing AI call never blocks the rest of Home). No fake coaching text on failure: graceful state + manual Retry only, no polling/auto-retry. CoachFetcherTest 8/8; the 4 FakeApi test impls gained a `getCoach` override. **Backend addition (owner-directed, for live testing only):** Gemini 2.5-flash free-tier DAILY quota (20 req/day/model) was exhausted mid-session (HTTP 429), so `coach/llm.py` gained `AgentRouterLLMProvider` (OpenAI-compatible /chat/completions) selected by the backend-only `LLM_PROVIDER` env switch (`agentrouter` vs default `gemini`) with `AGENTIC_API_KEY`/`AGENTROUTER_BASE_URL`/`AGENTROUTER_MODEL` in config; embeddings stay on Gemini (frozen 1536-dim pgvector). **agentrouter.org requires `User-Agent: claude-cli/1.0.0 (external, cli)`** or it 401s ("unauthorized client detected") — an app-level client filter, verified live. Device re-verification 4/4 PASS ran on DeepSeek (deepseek-v4-flash): SUCCESS / BACKEND FAILURE / RETRY / PERSONALIZATION (context 0 steps → STARTER; after 3,000-step run-sync → RECOVERY; reset → STARTER). Evidence: `docs/docs/ui/evidence-4c3b/`. ADR `docs/docs/ui/0002-android-coach-integration.md`. Backend 198 tests, tools 129, Android unit suite green, debug build OK. TTS remains deferred. No Git operations.

  - M8.3B (2026-09-06, Android receive-only): real-time coaching WebSocket
    client. New `core/network/CoachingWsClient.kt` (idempotent
    connect/disconnect, capped exp backoff 1s→30s ≤5 retries then quiet
    DISCONNECTED; sealed `LiveCoach/Ignored/Malformed` parse; only
    `coaching_message`'s `coach` → EXISTING `CoachResponse` is published —
    raw `coaching_trigger`/unknown/malformed frames are ignored, never crash),
    `OkHttpCoachingSocketFactory.kt` (`ws`/`wss` derived from
    `BuildConfig.BACKEND_BASE_URL` — `HttpUrl` only parses, `.scheme("ws")`
    builder throws), `LiveCoachStore.kt` (process-lifetime live slot,
    `StateFlow<CoachResponse?>`). Koin: one WS singleton on `AppScope`; WS
    OkHttp client pingInterval 30s, read/write timeout 0. MainActivity connects
    onStart / disconnects onStop (tab nav never duplicates). HomeTab prefers
    live push (`"⚡ Live coaching"`) over pull CoachCache — **separate live
    store; a push must NOT be written into pull CoachCache** (corrupts its
    synced-run-id signature bookkeeping). Unit tests 21 new (no network:
    scripted fake socket + Unconfined + no-op sleeper); full Android suite
    96 tests 0 failures; assembleDebug OK. **Real-device E2E PASS (Samsung
    RZ8R90661CF)**: the initially running backend on `:8000` (PID 24672,
    started 18:40, no `--reload`) predated M8.2 and 403'd all WS upgrades (the
    client handled it: capped-backoff retries → quiet give-up, no crash, REST
    intact); after owner-approved restart onto current `main`
    (`apps/api/.venv/Scripts/python.exe -m uvicorn app.main:app`) the device WS
    reached CONNECTED and a fresh 1,500-step run sync produced a real
    `coaching_message` rendered as **"⚡ Live coaching"** (evidence
    `docs/docs/ui/evidence-m83b/home_live_push.png`). ADR
    `docs/docs/coaching/0004-m83b-android-websocket-client.md`. M8.4 TTS still
    deferred. No Git operations.

  - M8.4 (2026-09-06, Android native TTS for live coaching): each genuinely NEW
    live `coaching_message` is now read aloud by the NATIVE engine
    (`android.speech.tts.TextToSpeech`) — no cloud TTS/API keys/external
    dependency, NO backend change. New `core/tts/`: `TtsSynthesizer.kt` (JVM
    seam: isReady/setOnReadyChanged/speak/stop/release, best-effort never
    throws), `AndroidTtsSynthesizer.kt` (real adapter; application context (no
    Activity leak); async race-safe init — onInit may be sync-in-constructor or
    later, SUCCESS-only readiness, default-locale setLanguage w/ engine
    fallback, QUEUE_FLUSH, utterance ids `fq-coach-N`, runCatching everywhere,
    TAG CoachingTts), `CoachingSpeechGate.kt` (pure bounded in-memory dedupe on
    context_fingerprint+normalized content — no DB; TTS-only markdown-light
    normalization of the message copy), `CoachingSpeechController.kt`
    (app-scoped Koin singleton collecting the EXISTING
    `LiveCoachStore.message.drop(1)` — REUSES the M8.3B state flow, NO parallel
    pipeline; drop(1) = retained/stale message never re-read on start/reconnect;
    latest-wins pending until engine ready; `enabled` seam defaults ON, no
    settings UI exists and none invented per spec; release() idempotent).
    FitQuestApp resolves + starts it once per process (guarded); AppModule has
    the three singles. Tests 21 new (8 gate + 13 controller over fake
    synthesizer + real LiveCoachStore, Unconfined): new/duplicate/different,
    stale-before-start never spoken, idempotent start, readiness-gated
    latest-wins, engine-never-ready + engine-error no-crash, disable/re-enable,
    release. Full Android suite 117 tests 0 failures (was 96); assembleDebug
    OK. **Real-device E2E PASS (Samsung RZ8R90661CF)**: fresh 2,000-step run
    sync → real coaching_message → `speak queued id=fq-coach-1` +
    `utterance started/done` (engine-level speech proof; media volume not
    boosted / no ear-confirmation); HOME+relaunch (WS IDLE→CONNECTED) → NOT
    re-spoken (one speak total); second fresh 3,500-step sync → NEW
    `speak queued id=fq-coach-2` + `utterance started`, and a UI dump confirmed
    the **"⚡ Live coaching"** card body == the spoken message. Evidence
    `docs/docs/ui/evidence-m84/m84-tts-live-card.{png,txt}`. ADR
    `docs/docs/coaching/0005-m84-tts.md`. Two dev-sandbox run-sync mutations
    (2,000 + 3,500 steps). No Git operations.

  - M9.3 (2026-09-07, PURE VERIFICATION — no code changes): Final pre-demo
    reliability & AI preflight, all 9 areas PASS. Backend boots via apps/api
    venv on :8000, /health OK, Supabase pooler connected, AI keys present
    (never printed). Dev user exists; alembic at head 0003; FK integrity 0
    orphans (all hexownership + runsession rows reference the dev user); no
    auto-seed needed (live DB already seeded). Live `GET /api/v1/coach` = 200
    grounded. Full live chain proven on-device: run finish → sync →
    workout_completed trigger → WS → PushCoach → RAG → LLM → new
    `coaching_message` rendered as "⚡ Live coaching" + native TTS spoke it.
    AI-failure probe (second uvicorn on :8001 with dead AGENTROUTER_BASE_URL):
    coach → graceful 502, server alive, run-sync replay `already_processed=true`
    (0 credit) — run_id idempotency live. Device sanity: launch→perms granted
    (FINE/COARSE/ACTIVITY_RECOGNITION/POST_NOTIFICATIONS)→Start Capture→ACTIVE
    RUN with FGS notification id=1001→Stop & Finish→"✓ Synced with server"→new
    live coach card; no FATAL, no M9.2 regression. Tests: backend pytest **391
    passed**, Android unit **153 passed (0 failures/0 errors/0 skipped)**,
    `assembleDebug` BUILD SUCCESSFUL. Dev-user lifetime 20,894; three disclosed
    run-sync pushes (1,200 + 2,000 + a 0-step device run) — no phantom
    territory. Backend still running on :8000. No Git operations. Full A–H
    report issued to owner.

  - M9.4 (2026-09-07, presentation polish — NO app/backend code changes): final
    demo-readiness pass. UI audit of all demo-path screens found ZERO
    presentation-breaking issues (every network card has terminal
    loading→success/error + Retry; finite 45 s read timeout > backend 30 s LLM
    timeout; no fake content, no forever-spinners). Full demo flow re-verified
    live on the Samsung device (RZ8R90661CF) against the running backend: cold
    launch → Home → Start Run → FGS id=1001 + HIGH_ACCURACY location live (M9.2
    re-arm works on-device) → Stop & Finish → "✓ Synced with server" → new
    "⚡ Live coaching" card + native TTS `utterance done id=fq-coach-1` →
    server leaderboard Rank #1 of 6. Demo data untouched (devuser 10 hexes,
    6 players, Lvl 11; one honest 0-step session row added by the verification
    run). Docs: README.md stale claims corrected (backend-connected state;
    dead dev-simulator instructions removed — `useDevLocation`/`useDevSteps`
    no longer exist in AppModule), NEW `docs/DEMO_DAY_CHECKLIST.md` (practical
    before-demo/demo-flow/contingency steps). Android 153 unit tests passed,
    assembleDebug BUILD SUCCESSFUL; backend suite not re-run (no backend files
    changed; M9.3 baseline 391). M9.4 DONE. Feature freeze intact; M9.2
    working-tree fixes untouched. No Git operations.
