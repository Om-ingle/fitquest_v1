# Copy this file to .env and set your local values.
MAPTILER_API_KEY="5LqIwCkHS7bVkv6Xjs90"
MAPTILER_STYLE_URL=https://api.maptiler.com/maps/streets/style.json?key=${MAPTILER_API_KEY}

# FastAPI backend base URL used for run sync (Android side only — never put
# DATABASE_URL or Supabase credentials here). The default targets the Android
# emulator's alias for the host machine's localhost:
#   emulator:  http://10.0.2.2:8000/
#   real device on the same Wi-Fi as the backend: http://<YOUR_PC_LAN_IP>:8000/
BACKEND_BASE_URL=http://10.0.2.2:8000/

